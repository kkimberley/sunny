<!DOCTYPE html>
<html>
  <head>
    <title>Sunny</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
    <%= stylesheet_pack_tag 'application', media: 'all' %>
  </head>

  <body>
    <div id="hello">
      <el-container direction="vertical">
        <site-header cost-path="<%= costs_path %>" current-path="<%= request.path %>"></site-header>
        <el-main>
          <%= yield %>
        </el-main>
      </el-container>
    </div>
  </body>
</html>
